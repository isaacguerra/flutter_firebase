import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:apptestefirebase/app/modules/noticias/noticias_page.dart';

main() {
  testWidgets('NoticiasPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NoticiasPage(title: 'Noticias')));
    //  final titleFinder = find.text('Noticias');
    //  expect(titleFinder, findsOneWidget);
  });
}
